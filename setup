#!/bin/bash

apt autoremove -y game-repo science-repo && apt update && apt full-upgrade -y && pkg install -y root-repo unstable-repo x11-repo && apt update && apt full-upgrade -y && apt install -y python wget git vim dnsutils nmap figlet toilet neofetch golang ruby golang-* openssl openssh && pip install lolcat bs4 requests cowsay python-nmap pymetasploit3 && wget -O nethunter https://offs.ec/2MceZWr && chmod +x nethunter && bash nethunter && echo -e "clear && nh -r" > .bashrc && echo -e "mkdir $HOME/.termux/ ;echo "extra-keys = [['F1','F2','F3','F4','F5','F6','F7'],['ESC','/','-','HOME','UP','END','PGUP'],['TAB','CTRL','ALT','LEFT','DOWN','RIGHT','PGDN']]" >> $HOME/.termux/termux.properties 
termux-reload-settings && sleep 1 && logout" > extra_keys && chmod +x extra_keys && echo -e "clear && apt update && apt full-upgrade -y && apt install -y vim python3 python3-pip figlet neofetch toilet medusa maven themole golang golang-* kali-tools-*" > $HOME/kali-arm64/root/nh_setup && chmod +x $HOME/kali-arm64/root/nh_setup && nh -r
