#!/bin/bash

apt autoremove -y game-repo science-repo && pkg update && apt full-upgrade -y && pkg install -y root-repo unstable-repo x11-repo && pkg update && apt full-upgrade -y && pkg install -y python wget git vim dnsutils nmap figlet toilet neofetch golang ruby golang-* openssl openssh && pip install lolcat bs4 requests cowsay python-nmap pymetasploit3 && wget -O nethunter https://offs.ec/2MceZWr && chmod +x nethunter && bash nethunter && echo -e "clear && nh -r" > .bashrc && echo -e "clear && update && apt full-upgrade -y && apt install -y vim python3 python3-pip figlet neofetch toilet medusa maven golang golang-* kali-tools-*" > $HOME/kali-arm64/root/nh_setup && chmod +x $HOME/kali-arm64/root/nh_setup && nh -r
