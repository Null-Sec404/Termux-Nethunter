#!/bin/bash

apt autoremove -y game-repo science-repo && apt update && apt full-upgrade -y && pkg install -y wget git python perl curl figlet neofetch toilet vim nmap dnsutils coreutils && wget -O wget -O nethunter https://offs.ec/2MceZWr && chmod +x nethunter && echo -e "clear && nh-r" > .bashrc && bash nethunter && echo -e "clear && apt update && apt upgrade -y && apt install -y python3 python3-pip medusa vim nmap themole golang golang-* kali-tools-* && apt update && apt full-upgrade -y" > $HOME/kali-arm64/root/nh_setup