#!/bin/bash

apt autoremove -y game-repo science-repo && apt update && apt full-upgrade -y && pkg install -y php apache2 wget git python perl curl proot openssl openssh figlet neofetch toilet vim nmap dnsutils coreutils && wget -O wget -O nethunter https://offs.ec/2MceZWr && chmod +x nethunter && echo -e "clear && nh -r" > .bashrc && bash nethunter && echo -e "clear && apt update && apt full-upgrade -y " > $HOME/kali-arm64/root/nh_update && echo -e "apt install -y kali-tools-* golang golang-* python3 python3-pip figlet neofetch toilet && pip install bs4 termcolor colored requests python-nmap pymetasploit3 lolcat cowsay && wpscan --update" > $HOME/kali-arm64/root/nh_setup && chmod +x $HOME/kali-arm64/root/nh_setup
